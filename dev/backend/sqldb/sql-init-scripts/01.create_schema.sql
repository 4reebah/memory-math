-- Drop database schema if exists
-- DROP SCHEMA IF EXISTS memory_math;

-- Create a new database schema
CREATE SCHEMA IF NOT EXISTS memory_math;

-- Drop exist user
-- DROP USER IF EXISTS 'memory_math'@'localhost';

-- Create new user
-- CREATE USER 'memory_math'@'localhost' IDENTIFIED BY 'memory_math';

-- Grant privileges for this user to new schema
-- GRANT ALL PRIVILEGES ON memory_math.* TO 'memory_math'@'localhost';